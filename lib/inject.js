// Generated by CoffeeScript 1.4.0
var argsOf;

argsOf = require('./util').argsOf;

exports.sync = function(signatureFn, fn) {
  return (function(context) {
    return function() {
      var arg, injected, _i, _len;
      injected = typeof signatureFn === 'function' ? signatureFn(argsOf(fn)) : signatureFn instanceof Array ? signatureFn : signatureFn instanceof Object ? context = {
        config: signatureFn
      } : [signatureFn];
      if (context == null) {
        for (_i = 0, _len = arguments.length; _i < _len; _i++) {
          arg = arguments[_i];
          injected.push(arg);
        }
        return fn.apply(null, injected);
      }
      return fn.apply(null, null);
    };
  })(void 0);
};

exports.async = function(signatureFn, fn) {
  return function() {
    var arg, context, original;
    context = void 0;
    original = (function() {
      var _i, _len, _results;
      _results = [];
      for (_i = 0, _len = arguments.length; _i < _len; _i++) {
        arg = arguments[_i];
        _results.push(arg);
      }
      return _results;
    }).apply(this, arguments);
    if (typeof signatureFn === 'function') {
      signatureFn(argsOf(fn).slice(1), function(error, result) {
        return fn.apply(null, [error].concat(result).concat(original));
      });
    } else if (signatureFn instanceof Array) {
      fn.apply(null, signatureFn.concat(original));
    } else if (signatureFn instanceof Object) {
      context = {
        config: signatureFn
      };
    } else {
      fn.apply(null, [signatureFn].concat(original));
    }
    if (context == null) {
      return;
    }
    return fn.apply(null, null);
  };
};
