// Generated by CoffeeScript 1.9.2
var Also, localExports, nodeExport,
  slice = [].slice;

localExports = {
  validate: {},
  inject: {},
  deferred: {},
  sequence: {},
  pipeline: {},
  parallel: {},
  When: {},
  enclose: {},
  schedule: {},
  match: {},
  util: {},
  root: {}
};

nodeExport = function(ontoObject) {
  var submoduleName;
  for (submoduleName in localExports) {
    Object.defineProperty(ontoObject, submoduleName, {
      enumerable: true,
      value: require("./" + submoduleName)
    });
  }
  return ontoObject;
};

Also = function() {
  var args, definitions, exporter, futureArgs, i, moduleFactoryFn, objectName, results, root;
  args = 1 <= arguments.length ? slice.call(arguments, 0) : [];
  exporter = args[0], futureArgs = 3 <= args.length ? slice.call(args, 1, i = args.length - 1) : (i = 1, []), moduleFactoryFn = args[i++];
  root = {
    context: {}
  };
  nodeExport(root);
  results = [];
  for (objectName in (definitions = moduleFactoryFn(root))) {
    results.push(exporter[objectName] = definitions[objectName]);
  }
  return results;
};

module.exports = nodeExport(Also);
